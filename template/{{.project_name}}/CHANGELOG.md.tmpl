# Changelog

Releases and notes are generated with [cocogitto](https://github.com/cocogitto/cocogitto) from conventional commits. Configure `owner` and `repository` in `cog.toml` before running `cog changelog`.
