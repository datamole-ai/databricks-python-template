# Cocogitto: conventional commits, changelog, and release notes.
# Replace owner/repository with your GitHub org and repo name.
tag_prefix = "v"
branch_whitelist = ["main", "release/**"]

[changelog]
path = "CHANGELOG.md"
template = "remote"
remote = "github.com"
owner = "OWNER"
repository = "REPO"
